<h2>Add a restaurant here</h2>

<%= simple_form_for @restaurant do |r| %>
  <%= r.input :name %>
  <%= r.input :address %>
  <%= r.input :phone_number %>
  <%= r.select :category, Restaurant::CATEGORY %>
  <%= r.button :submit %>
<% end %>
